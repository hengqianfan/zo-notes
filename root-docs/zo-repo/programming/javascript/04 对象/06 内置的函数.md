```js
let person = {
    name:'momo',
    age:18
}
// 获取对象的所有键名（属性名）
let arr = Object.keys(person)
// 获取对象的所有属性值
let arr = Object.values(person)
// 对象拷贝（复制、备份）
let newobj = {}
Object.assign( newarr, person )
```